# Global Docker ignore to keep context small and avoid missing files issues
# IMPORTANT: Build context must be the repository root for backend Dockerfile

# Node
node_modules
npm-debug.log*
yarn.lock
pnpm-lock.yaml

# Frontend build artifacts
frontend/node_modules
frontend/dist

# VCS
.git
.gitignore

# OS files
.DS_Store
Thumbs.db
